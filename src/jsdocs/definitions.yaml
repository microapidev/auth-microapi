definitions:
  # BadRequestResponse model
  BadRequestResponse:
    type: object
    properties:
      status:
        type: string
        example: error
      error:
        type: string
        example: Invalid body supplied
