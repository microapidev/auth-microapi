definitions:
  # BadRequest model
  BadRequest:
    type: object
    properties:
      status:
        type: string
        example: error
      error:
        type: string
        example: Invalid body supplied
